export const json_post = (url, datas, {onSuccess, onError, onComplete}) => {};
export const json_post_dispatch_all = (url, datas) => {};

export default {
    json_post,
    json_post_dispatch_all,
}
