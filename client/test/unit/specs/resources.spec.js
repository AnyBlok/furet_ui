import { getRoutes } from '@/resources';
import { createRouter } from '@/router';
import { defineResource } from '@/resources/factory';

describe('components factory', () => {

  it('createComponents about', () => {
    const routes = getRoutes();
  });
});
